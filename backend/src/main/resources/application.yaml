server:
  port: 8080
spring:
  application:
    name: bootcamp_db
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/bootcamp_db
    username: dev_beam
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true